<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>{% block head_title %}PDF preview{% endblock %}</title>
	<style>
		html, body {
			width: 100%;
			height: 100%;
			font-family: sans-serif;
			font-size: 62.5%;
			margin: 0px;
			padding: 0px;
		}
		#wrapper {
			margin: 2% auto 5% auto;
			width: 95%;
			min-height: 90%;
			position: relative;
		}
		.main-logo {
			display: block;
			margin-left: auto;
			margin-right: auto;
		}
		.main-title {
			font-size: 5em;
			font-weight: 100;
			text-align: center;
		}
		.section-header {
			background-color: #949494;
			color: #ffffff;
			color: #333333;
			padding: 1em;
			margin-bottom: 1em;
		}
		.section-header h2 {
			font-size: 3em;
			font-weight: 100;
			margin: 0px;
		}
		.section-content {
			font-size: 2.3em;
		}
		.footnote {
			width: 80%;
			margin-left: auto;
			margin-right: auto;
			font-size: 0.8em;
			background-color: #ccc;
			padding: 0.2em 0.5em 0.2em 0.5em;
			box-sizing: border-box;
		}
		.main-footer {
			text-align: center;
			font-size: 2em;
			width: 100%;
			position: absolute;
			bottom: -2em;
		}
		.chart {
			margin-left: auto;
			margin-right: auto;
			width: 680px;
			min-height: 450px;
			height: 450px;
			border: 1px solid #ccc;
			padding: 2em;
		}
		.clipper-charts-npschart, .clipper-charts-heatmap, .clipper-charts-dnachart, .clipper-charts-promvsdetrpromotechart {
			height: auto;
		}
		.clipper-charts-promoterspromotechart-cell, .clipper-charts-detractorspromotechart-cell, .clipper-charts-ppdbrandmessageschart-maintable tr {
			page-break-inside: avoid;
		}
		.clipper-charts-heatmap > div > div:last-child {
			bottom: -1em !important;
		}
		.clipper-charts-promvsdetrpromotechart-brand {
			margin: 1%;
			padding: 1%;
			width: 45%;
			display: inline-block;
			border: 1px solid #ccc;
			page-break-inside: avoid;
		}
		.clipper-charts-dnachart > div {
			height: auto !important;
		}
		.clipper-charts-dnachart-body {
			height: auto !important;
		}
	</style>
	<script>
	{{ file('@PSLClipperBundle/Resources/public/js/clipper.charts.js')|raw }}
	</script>
	<script>
	{{ file('@PSLClipperBundle/Resources/public/js/charts.pdf.config.js')|raw }}
	</script>
	{# @todo: add/load from vendor folder using composer.json #}
    <!--script type="text/javascript" src="{#{ app.request.scheme ~ '://' ~ 'localhost:8081' ~ asset('bundles/pslclipper/js/clipper.charts.js') }#}"></script>
    {# specific rules for pdf downloads #}
    <script type="text/javascript" src="{#{ app.request.scheme ~ '://' ~ 'localhost:8081' ~ asset('bundles/pslclipper/js/charts.pdf.config.js') }#}"></script-->
</head>
<body>

	<div id="wrapper">
		{% block section %}
			{% block header %}
				<header class="section-header">
					<h2>{{ section_title|e('html') }}</h2>
				</header>
			{% endblock %}
			<div class="section-content">
				{% block content %}
					{{ content|raw }}
				{% endblock %}
			</div>
		{% endblock %}
		<footer class="main-footer">
			{% block footer %}
				<span class="page"></span>
			{% endblock %}

		</footer>
	</div>
	
</body>
</html>